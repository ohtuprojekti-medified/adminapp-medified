version: '3.8'

services:

  adminapp-db:
    image: postgres:latest
    restart: unless-stopped
    env_file: ./.env
    volumes:
      - database-data:/var/lib/postgresql/data
    ports:
      - 5432:5432

  adminapp-backend:
    build: backend
    ports:
      - 5000:5000
    env_file: ./.env
    environment:
      - PORT=5000
      - NODE_ENV=development
    depends_on:
      - adminapp-db
    volumes:
     - ./backend:/usr/local/app
    command: npm run dev
    container_name: adminapp-backend
    
  adminapp-frontend:
    build: frontend
    ports:
      - 3000:3000
    depends_on:
      - adminapp-backend
    volumes:
      - ./frontend:/usr/local/app
    command: npm start
    container_name: adminapp-frontend

volumes:
  database-data: